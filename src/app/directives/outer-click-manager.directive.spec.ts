import { OuterClickManagerDirective } from './outer-click-manager.directive';

describe('OuterClickManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new OuterClickManagerDirective();
    expect(directive).toBeTruthy();
  });
});
